dependencies:
  pre:
    - sudo add-apt-repository -y ppa:blinxwang/mpv-builds
    - sudo add-apt-repository -y ppa:duggan/bats
    - sudo apt-get update
    - sudo apt-get install bats
    - sudo apt-get install jq
    - sudo apt-get install mpv

test:
  override:
    - make test
