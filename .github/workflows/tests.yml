name: code-tests
on: push

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: install shellcheck
        run: sudo apt-get install -y shellcheck
      - name: doshellcheck
        run: shellcheck -V && shellcheck src/somafm && echo "shellcheck OK"

